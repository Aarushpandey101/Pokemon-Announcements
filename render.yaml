services:
  - type: web
    name: discord-announce-watcher
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 discord_announce_watcher.py
    healthCheckPath: /health